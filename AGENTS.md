# Base for Anything 작업 지침

## 프로젝트 목적

- 이 저장소는 코딩 초보자가 Codex와 함께 작은 웹 서비스를 만드는 베이스 키트다.
- 사용자가 직접 코드를 작성하는 것보다 원하는 기능을 말하고 결과를 확인하는 흐름을 우선한다.
- 작고 작동하는 결과를 한 단계씩 완성한다.

## 작업 전 확인

- 모든 작업 전에 `PROJECT_BRIEF.md`를 확인한다.
- 프로젝트 시작 인터뷰는 `guides/project-start-flow.md`를 따른다.
- 오늘의 주문 인터뷰는 `guides/order-flow.md`를 따른다.
- 날짜별 주문 기록은 `orders/`에서 확인한다.

## 범위 통제

- 한 번에 한 가지 작업만 처리하고 요청하지 않은 기능은 추가하지 않는다.
- 기존에 동작하는 기능을 유지하고 관련 없는 파일은 리팩터링하지 않는다.
- 복잡한 상태 관리, 조기 추상화, 불필요한 라이브러리를 추가하지 않는다.
- 새로운 production dependency가 정말 필요하면 설치 전에 이유를 짧게 설명하고 확인받는다.
- 중요하고 되돌리기 어려운 선택은 임의로 결정하지 않는다. 쉽게 되돌릴 수 있는 문구와 명칭은 합리적으로 진행한다.
- 범위가 넓으면 가장 작은 완료 단위로 나누어 제안한다.

## 기술 원칙

- Next.js 16 App Router와 필요한 경우 Promise 기반 `params`를 유지한다. 오래된 Next.js 13/14 패턴으로 바꾸지 않는다.
- 인증에는 `@supabase/ssr`을 사용하고 `middleware.ts` 대신 `proxy.ts`를 사용한다.
- Supabase RLS를 우회하지 않으며 service role key를 클라이언트 코드에 사용하지 않는다.
- 카메라와 Supabase Storage는 요청 전까지 구현하지 않는다.
- 초보자가 역할을 찾기 쉬운 짧고 명시적인 파일 구조를 유지한다.

## 문서와 대화 원칙

- 사용자와의 대화는 기본적으로 한국어로 진행하고 질문은 한 번에 하나씩 한다.
- 이미 말한 내용은 다시 질문하지 않고, 불분명한 중요한 항목만 짧게 묻는다.
- 코드 작업 전 주문 내용을 문서로 정리하여 확인받는다.
- 설명은 짧고 구체적으로 하며 요청하지 않은 추가 기능을 제안하지 않는다.
- 작업 후 변경 파일, 확인 방법, 실행 결과만 간단히 보고한다.

## 완료 조건

- 관련 lint와 build를 실행하고 가능한 경우 실제 동작을 확인한다.
- 오류는 숨기지 않고 원인을 설명한다.
- 작업 결과를 해당 주문서에 기록한다.
- 완료 범위를 넘어 추가 작업하지 않고 멈춘다.
